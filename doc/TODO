Install PostGIS:
https://www.openshift.com/blogs/time-for-a-spatial-power-up-openshift-postgis

Fix django-leaflet
* Install the module so it doesn't break django
* Uncomment the variables in the template

Data Download:
* Update script to download into openshift perstent data folder
* Load into DB
* Need to be inserted as python objects using Django calls so that it goes 
  through the ORM?

Data import as a cron job:
* OpenShift cron cartridge for scheduled jobs
* For multiple jobs might be easier to use django-chronograph
  (so need just one cron job)

Display food data as a list on the site

Allow user to select a restaurant and see data

Allow ratings and comments?

Long term:
* Set admin email and debug email to a team mailing list
* South for migrating the database
* Backup the database (psql dump over SSH)
* Blob store or shared storage for uploaded files
* Use mongo for recording history of queries / what people search?
